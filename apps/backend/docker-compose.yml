services:
  redis:
    image: redis:alpine
    container_name: renjana
    ports:
      - '6379:6379'
    volumes:
      - renjana_redis_data:/data
    restart: unless-stopped
volumes:
  renjana_redis_data: